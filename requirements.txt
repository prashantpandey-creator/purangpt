# ── Backend & Database ───────────────────────────────────────────────────
fastapi==0.115.5
uvicorn==0.32.1
supabase==2.31.0
psycopg2-binary==2.9.9
asyncpg==0.30.0
python-multipart==0.0.12
sse-starlette==2.1.3
duckduckgo-search>=5.0.03

# ── RAG & LLM Framework ────────────────────────────────────────────────────
llama-index-core==0.12.8
llama-index-llms-ollama==0.5.0
llama-index-llms-openai==0.3.18
llama-index-embeddings-huggingface==0.5.0

# ── Keyword Search ─────────────────────────────────────────────────────────
rank-bm25==0.2.2

# ── Vector Search (ChromaDB + Embeddings) ──────────────────────────────────

sentence-transformers>=3.0.0

# ── PDF Processing ─────────────────────────────────────────────────────────
PyMuPDF==1.24.14

# ── OCR (Devanagari / Sanskrit) ────────────────────────────────────────────
paddleocr==2.9.1
paddlepaddle==2.6.2

# ── Computer Vision (OCR preprocessing) ───────────────────────────────────
opencv-python-headless==4.10.0.84

# ── Embeddings ─────────────────────────────────────────────────────────────
# Removed sentence-transformers to save ~1.5GB RAM and disk space on Railway

# ── OpenAI-compatible client (Groq API) ────────────────────────────────────
openai==1.58.1

# ── Web Scraping / Downloading ─────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
aiohttp==3.11.9
aiofiles==24.1.0

# ── Utilities ──────────────────────────────────────────────────────────────
indic-transliteration>=2.3.57
python-dotenv==1.0.1
gunicorn>=21.2.0
tqdm==4.67.1
numpy==1.26.4
Pillow==11.0.0

# ── Desktop App ────────────────────────────────────────────────────────────
pywebview==5.3.2

supabase>=2.0.0
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0
pinecone>=3.0.0
stripe>=5.0.0
razorpay>=1.3.0
rich==13.9.4
gunicorn>=21.2.0

# ── Caching ────────────────────────────────────────────────────────────────
redis>=5.0.0
