{
  "name": "purangpt-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor iOS shell for PuranGPT (remote-URL mode). Minimal deps — only what cap sync + Package.swift need.",
  "scripts": {
    "cap:webdir": "node scripts/cap-webdir.mjs",
    "cap:sync:ios": "npm run cap:webdir && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/app": "8.1.0",
    "@capacitor/browser": "8.0.3",
    "@capacitor/core": "8.4.0",
    "@capacitor/ios": "8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.0",
    "typescript": "^5"
  }
}
